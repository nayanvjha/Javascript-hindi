//console.log("2" > 1);
//console.log("02" > 1);

console.log(null > 0);  // these type of conversions are always confusing where both the data types in comparison are not same

console.log(null == 0);

console.log(null >= 0);


// === it strictly checks i.e it checks both the values and its data type
